import React from "react";

export default function OptionItem(props) {
    //still there is only one option in this menu I wouldn't pass it through props 
    return (
        <li>Logout</li>
    )
}